# p5.js Doodle Library

Minimal Vite + TypeScript sandbox for p5.js doodles.

## Run

```bash
npm install
npm run dev
```

## Add a doodle

Create a new file in `src/doodles/` that exports `name` and `sketch`.
